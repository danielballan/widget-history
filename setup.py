from setuptools import setup

setup(name='widget_history',
      version='0.1.0',
      author='Dan Allan',
      py_modules=['widget_history'],
      description='static, persistent widgets',
      url='http://github.com/danielballan/widget_history',
      platforms='Cross platform (Linux, Mac OSX, Windows)',
      requires=['IPython']
      )
